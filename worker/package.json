{
  "name": "de-luisa-bio-admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun dev-server.ts",
    "deploy": "wrangler deploy --autoconfig=false --config wrangler.toml"
  }
}
